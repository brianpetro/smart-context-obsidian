export const context_codeblock_types = Object.freeze([
  'ctx',
  'smart-context',
  'context',
]);

export const context_named_context_prefixes = Object.freeze([
  'smart-context::',
  'ctx::',
  'context::',
]);

export const default_context_codeblock_type = 'ctx';
export const default_named_context_line_prefix = 'smart-context';
